// Package app holds one use case per verb×noun cell (docs/tdd.md §9) and owns the Plan type
// (docs/tdd.md §4). Ports are declared as interfaces at the point of consumption rather than
// centralized in a separate abstractions module (T13).
//
// Empty until M1.
package app
